url: https://aroacing-the-internet.netlify.app # replace with your webring's URL, which should end in .netlify.app (no slash afterwards)
include: [_redirects]
github_repo_url: https://github.com/gael10204/aroacing-the-internet # replace with your repository's URL, which should begin with https://github.com/ and should also have no slash at the end

